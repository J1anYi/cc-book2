---
phase: 14-collection-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/models/book.ts
  - backend/src/validators/schemas.ts
  - backend/src/routes/collections.ts
  - backend/src/routes/books.ts
  - backend/src/index.ts
autonomous: true
requirements:
  - COLL-01
  - COLL-02
  - COLL-03
  - COLL-04
  - COLL-05
  - COLL-06

must_haves:
  truths:
    - "User can create a collection with name, icon, and color"
    - "User can rename and delete collections"
    - "User can add a book to multiple collections"
    - "User can remove a book from a collection"
    - "Deleting a collection does not delete the books"
    - "Collection list shows book count per collection"
    - "Books can be filtered by collection via API"
  artifacts:
    - path: "backend/src/routes/collections.ts"
      provides: "Collection CRUD API endpoints"
      exports: ["router"]
    - path: "backend/src/models/book.ts"
      provides: "collections and book_collections tables"
      contains: "CREATE TABLE IF NOT EXISTS collections"
    - path: "backend/src/validators/schemas.ts"
      provides: "Collection validation schemas"
      contains: "collectionSchema"
  key_links:
    - from: "backend/src/routes/collections.ts"
      to: "backend/src/models/book.ts"
      via: "db() function"
      pattern: "import { db } from '../models/book.js'"
    - from: "backend/src/routes/books.ts"
      to: "book_collections table"
      via: "SQL JOIN"
      pattern: "JOIN book_collections"
    - from: "backend/src/index.ts"
      to: "collectionsRouter"
      via: "app.use"
      pattern: "app.use('/api/collections'"
---

<objective>
Implement backend infrastructure for collection system: database schema, validation schemas, and full REST API for collection CRUD and book-to-collection assignment.

Purpose: Enable many-to-many relationship between books and collections with proper referential integrity and CASCADE DELETE behavior.
Output: Working backend API for all collection operations (COLL-01 through COLL-06).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-collection-system/14-RESEARCH.md

<interfaces>
<!-- Existing patterns executor must follow -->

From backend/src/routes/categories.ts:
```typescript
import { Router } from 'express';
import { db } from '../models/book.js';
import { authMiddleware } from '../middleware/auth.js';
import { validateBody, validateParams } from '../middleware/validate.js';
import { categorySchema, idParamSchema } from '../validators/schemas.js';

const router = Router();
router.get('/', (req, res) => { ... });
router.post('/', authMiddleware, validateBody(categorySchema), (req, res) => { ... });
export default router;
```

From backend/src/models/book.ts:
```typescript
// SQLiteDatabase class with run(), get(), all(), exec(), save() methods
// initDatabase() creates tables with dbInstance.exec()
// getDb() exported as db for sync access
```

From backend/src/validators/schemas.ts:
```typescript
export const categorySchema = z.object({
  name: z.string().min(1).max(100),
  description: z.string().max(500).optional(),
});
export const idParamSchema = z.object({
  id: z.coerce.number().int().positive(),
});
```

From backend/src/index.ts:
```typescript
import collectionsRouter from './routes/collections.js';
app.use('/api/collections', collectionsRouter);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add collection tables to database schema</name>
  <files>backend/src/models/book.ts</files>
  <read_first>
    - backend/src/models/book.ts (existing schema pattern)
  </read_first>
  <behavior>
    - Test 1: collections table exists with columns: id, name, description, icon, color, created_at
    - Test 2: book_collections junction table exists with composite primary key
    - Test 3: CASCADE DELETE works - deleting collection removes book_collections entries
    - Test 4: UNIQUE constraint on collections.name prevents duplicates
  </behavior>
  <action>
    In backend/src/models/book.ts, inside initDatabase() function, after the highlights table creation (around line 151), add two new tables:

    1. collections table:
       - id INTEGER PRIMARY KEY AUTOINCREMENT
       - name TEXT NOT NULL UNIQUE
       - description TEXT
       - icon TEXT (for emoji, max ~10 chars)
       - color TEXT (for hex color like #RRGGBB)
       - created_at DATETIME DEFAULT CURRENT_TIMESTAMP

    2. book_collections junction table:
       - book_id INTEGER NOT NULL
       - collection_id INTEGER NOT NULL
       - added_at DATETIME DEFAULT CURRENT_TIMESTAMP
       - PRIMARY KEY (book_id, collection_id)
       - FOREIGN KEY (book_id) REFERENCES books(id) ON DELETE CASCADE
       - FOREIGN KEY (collection_id) REFERENCES collections(id) ON DELETE CASCADE

    3. Add indexes:
       - CREATE INDEX IF NOT EXISTS idx_book_collections_book ON book_collections(book_id)
       - CREATE INDEX IF NOT EXISTS idx_book_collections_collection ON book_collections(collection_id)

    Call dbInstance.exec() for each CREATE TABLE and CREATE INDEX statement, following the exact pattern used for existing tables.
  </action>
  <verify>
    <automated>grep -c "CREATE TABLE IF NOT EXISTS collections" backend/src/models/book.ts | grep -v '^0$' && grep -c "CREATE TABLE IF NOT EXISTS book_collections" backend/src/models/book.ts | grep -v '^0$'</automated>
  </verify>
  <done>Database schema includes collections and book_collections tables with proper foreign keys and indexes</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add collection validation schemas</name>
  <files>backend/src/validators/schemas.ts</files>
  <read_first>
    - backend/src/validators/schemas.ts (existing schema pattern)
  </read_first>
  <behavior>
    - Test 1: collectionSchema validates name (required, 1-100 chars)
    - Test 2: collectionSchema validates description (optional, max 500 chars)
    - Test 3: collectionSchema validates icon (optional, max 10 chars)
    - Test 4: collectionSchema validates color (optional, hex format #RRGGBB or empty)
    - Test 5: bookQuerySchema includes optional collection_id parameter
  </behavior>
  <action>
    In backend/src/validators/schemas.ts, add the following schemas after categorySchema (around line 14):

    1. collectionSchema:
       ```typescript
       export const collectionSchema = z.object({
         name: z.string().min(1, '名称不能为空').max(100, '名称不能超过100字符'),
         description: z.string().max(500, '描述不能超过500字符').optional(),
         icon: z.string().max(10, '图标不能超过10字符').optional(),
         color: z.string().regex(/^#[0-9A-Fa-f]{6}$|^$/, '颜色格式无效').optional(),
       });
       ```

    2. Add collection_id to bookQuerySchema (modify existing schema around line 42):
       - Add: collection_id: z.coerce.number().int().positive().optional()

    3. Add type export:
       ```typescript
       export type CollectionInput = z.infer<typeof collectionSchema>;
       ```

    Follow the exact pattern of existing schemas - use z.string(), z.coerce.number(), etc.
  </action>
  <verify>
    <automated>grep -c "collectionSchema" backend/src/validators/schemas.ts | grep -v '^0$' && grep -c "collection_id" backend/src/validators/schemas.ts | grep -v '^0$'</automated>
  </verify>
  <done>Zod schemas for collection CRUD and book query filtering exist with proper validation rules</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create collections route with full CRUD API</name>
  <files>backend/src/routes/collections.ts</files>
  <read_first>
    - backend/src/routes/categories.ts (exact pattern to follow)
    - backend/src/routes/highlights.ts (for error handling patterns)
  </read_first>
  <behavior>
    - Test 1: GET /api/collections returns list with book_count per collection
    - Test 2: POST /api/collections creates collection, returns 201
    - Test 3: POST /api/collections with duplicate name returns 409
    - Test 4: PUT /api/collections/:id updates collection
    - Test 5: DELETE /api/collections/:id removes collection and cascades to book_collections
    - Test 6: POST /api/collections/:id/books/:bookId adds book to collection
    - Test 7: DELETE /api/collections/:id/books/:bookId removes book from collection
  </behavior>
  <action>
    Create backend/src/routes/collections.ts following the exact pattern from categories.ts. Implement these endpoints:

    1. GET / - List all collections with book counts:
       - SQL: SELECT c.*, COUNT(bc.book_id) as book_count FROM collections c LEFT JOIN book_collections bc ON c.id = bc.collection_id GROUP BY c.id ORDER BY c.name
       - Return: res.json(collections)

    2. POST / - Create collection (authMiddleware, validateBody):
       - Extract name, description, icon, color from req.body
       - INSERT INTO collections, call database.save()
       - Handle SQLITE_CONSTRAINT with 409 status
       - Return: res.status(201).json({ id, name, description, icon, color, book_count: 0 })

    3. PUT /:id - Update collection (authMiddleware, validateParams):
       - Check collection exists (404 if not)
       - UPDATE collections SET name=?, description=?, icon=?, color=? WHERE id=?
       - Handle SQLITE_CONSTRAINT for duplicate name
       - Return updated collection with book_count

    4. DELETE /:id - Delete collection (authMiddleware, validateParams):
       - Check collection exists (404 if not)
       - DELETE FROM collections WHERE id=? (CASCADE handles book_collections)
       - Call database.save()
       - Return: res.json({ success: true, message: '收藏夹已删除' })

    5. POST /:id/books/:bookId - Add book to collection (authMiddleware, validateParams):
       - Verify book exists (404 if not)
       - Verify collection exists (404 if not)
       - INSERT INTO book_collections (book_id, collection_id) VALUES (?, ?)
       - Handle SQLITE_CONSTRAINT_PRIMARYKEY as idempotent success (already in collection)
       - Return: res.status(201).json({ success: true, message: '已添加到收藏夹' })

    6. DELETE /:id/books/:bookId - Remove book from collection (authMiddleware, validateParams):
       - DELETE FROM book_collections WHERE collection_id=? AND book_id=?
       - Call database.save()
       - Return: res.json({ success: true, message: '已从收藏夹移除' })

    Import collectionSchema and idParamSchema from validators/schemas.ts.
    Use validateBody(collectionSchema) for POST, validateParams(idParamSchema) for routes with :id.
    Always call database.save() after write operations.
  </action>
  <verify>
    <automated>grep -c "router.get('/'" backend/src/routes/collections.ts | grep -v '^0$' && grep -c "router.post('/'" backend/src/routes/collections.ts | grep -v '^0$' && grep -c "book_collections" backend/src/routes/collections.ts | grep -v '^0$'</automated>
  </verify>
  <done>Complete REST API for collections with CRUD and book assignment endpoints</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Add collection filter to books route</name>
  <files>backend/src/routes/books.ts</files>
  <read_first>
    - backend/src/routes/books.ts (existing GET / route)
  </read_first>
  <behavior>
    - Test 1: GET /api/books?collection_id=1 returns only books in that collection
    - Test 2: GET /api/books?collection_id=1&search=term combines both filters
    - Test 3: Collection filter uses JOIN with book_collections table
  </behavior>
  <action>
    In backend/src/routes/books.ts, modify the GET / route to support collection_id query parameter:

    1. Destructure collection_id from req.query alongside existing parameters:
       - const { page = 1, limit = 20, search, collection_id } = req.query as any;

    2. Modify the SQL query construction to conditionally JOIN book_collections:
       - If collection_id is provided, add: JOIN book_collections bc ON b.id = bc.book_id
       - Add condition: bc.collection_id = ?
       - Use SELECT DISTINCT b.* to avoid duplicates

    3. Update the count query similarly to include the JOIN and filter

    4. Ensure the query parameter is validated via bookQuerySchema (already added in Task 2)

    The pattern should be:
    - Start with base SQL: SELECT DISTINCT b.* FROM books b
    - If collection_id: append JOIN and add WHERE condition
    - Chain with existing search filter using AND
    - Apply ORDER BY, LIMIT, OFFSET at the end

    Do NOT break existing search and category filtering functionality.
  </action>
  <verify>
    <automated>grep -c "collection_id" backend/src/routes/books.ts | grep -v '^0$' && grep -c "book_collections" backend/src/routes/books.ts | grep -v '^0$'</automated>
  </verify>
  <done>Books API supports filtering by collection_id parameter</done>
</task>

<task type="auto">
  <name>Task 5: Register collections router in Express app</name>
  <files>backend/src/index.ts</files>
  <read_first>
    - backend/src/index.ts (existing router registration pattern)
  </read_first>
  <action>
    In backend/src/index.ts:

    1. Add import statement after existing route imports (around line 11):
       ```typescript
       import collectionsRouter from './routes/collections.js';
       ```

    2. Add route registration after existing app.use statements (around line 56):
       ```typescript
       app.use('/api/collections', collectionsRouter);
       ```

    Follow the exact pattern of existing router imports and registrations.
  </action>
  <verify>
    <automated>grep -c "collectionsRouter" backend/src/index.ts | grep -v '^0$' && grep -c "'/api/collections'" backend/src/index.ts | grep -v '^0$'</automated>
  </verify>
  <done>Collections router registered at /api/collections endpoint</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API | Untrusted input for collection name, description, icon, color |
| API → Database | SQL injection risk (mitigated by prepared statements) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | POST /api/collections | mitigate | Zod validation on name (1-100 chars), description (max 500), icon (max 10), color (hex regex) |
| T-14-02 | Tampering | SQL queries | mitigate | Prepared statements via db.run(sql, params) pattern |
| T-14-03 | Information Disclosure | GET /api/collections | accept | Collection names not sensitive, auth not required for read |
| T-14-04 | Tampering | DELETE /api/collections/:id | mitigate | authMiddleware ensures only authenticated users can delete |
| T-14-05 | Tampering | POST /:id/books/:bookId | mitigate | Verify book and collection exist before assignment, authMiddleware |
| T-14-06 | Repudiation | All write operations | accept | No audit logging required for personal book collection |
</threat_model>

<verification>
1. Start backend server: `cd backend && npm run dev`
2. Test collection CRUD:
   - POST /api/collections with name "Favorites" → 201
   - GET /api/collections → includes new collection with book_count: 0
   - PUT /api/collections/1 with new name → 200
   - DELETE /api/collections/1 → 200
3. Test book assignment:
   - POST /api/collections/1/books/1 → 201
   - GET /api/collections → book_count: 1
   - DELETE /api/collections/1/books/1 → 200
4. Test filtering:
   - GET /api/books?collection_id=1 → filtered results
5. Test CASCADE DELETE:
   - Create collection, add book, delete collection
   - Verify book still exists, book_collections entry removed
</verification>

<success_criteria>
1. Database has collections and book_collections tables with proper foreign keys
2. All 6 collection API endpoints return correct status codes and data
3. Book filtering by collection_id works correctly
4. CASCADE DELETE removes book_collections entries when collection is deleted
5. UNIQUE constraint prevents duplicate collection names with 409 response
6. All write operations call database.save() for persistence
</success_criteria>

<output>
After completion, create `.planning/phases/14-collection-system/14-01-SUMMARY.md`
</output>
