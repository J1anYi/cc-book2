---
phase: 15-reading-status
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/models/book.ts
  - backend/src/validators/schemas.ts
  - backend/src/routes/books.ts
autonomous: true
requirements:
  - STAT-01
  - STAT-02
  - STAT-04

must_haves:
  truths:
    - "Books have a reading_status column with values want_to_read, reading, or read"
    - "API endpoint PUT /api/books/:id/status updates reading status"
    - "API endpoint GET /api/books?status=reading filters books by status"
    - "Status filter returns only books matching the requested status"
  artifacts:
    - path: "backend/src/models/book.ts"
      provides: "reading_status column in books table"
      contains: "reading_status"
    - path: "backend/src/validators/schemas.ts"
      provides: "readingStatusSchema and updated bookQuerySchema"
      exports: ["readingStatusSchema", "bookQuerySchema"]
    - path: "backend/src/routes/books.ts"
      provides: "PUT /:id/status endpoint and status filter in GET /"
      contains: "PUT /:id/status"
  key_links:
    - from: "backend/src/routes/books.ts"
      to: "backend/src/validators/schemas.ts"
      via: "import readingStatusSchema"
      pattern: "import.*schemas"
    - from: "backend/src/routes/books.ts"
      to: "backend/src/models/book.ts"
      via: "db() getter"
      pattern: "db\\(\\)"
---

<objective>
Implement backend support for reading status tracking: database column, validation schema, and API endpoints for status update and filtering.

Purpose: Enable three-state reading progress tracking (want_to_read, reading, read) with manual update and list filtering.
Output: Working backend API for reading status management.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-reading-status/15-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add reading_status column to books table</name>
  <files>backend/src/models/book.ts</files>
  <read_first>
    - backend/src/models/book.ts (current schema and migration pattern)
    - .planning/phases/15-reading-status/15-RESEARCH.md (section 1: Database Schema Changes)
  </read_first>
  <action>
    Add reading_status column to books table with default value 'want_to_read'. Follow the pattern from RESEARCH.md section 1:
    
    1. After the book_collections table creation (around line 175), add column check and migration:
       - Check if reading_status column exists using PRAGMA table_info(books)
       - If missing, execute ALTER TABLE books ADD COLUMN reading_status TEXT DEFAULT 'want_to_read'
    
    2. The column stores TEXT values: 'want_to_read', 'reading', 'read'
    
    3. Call database.save() after migration.
    
    Do NOT add CHECK constraint at database level (SQLite limitation). Validation will be enforced in Zod schema.
  </action>
  <acceptance_criteria>
    - File backend/src/models/book.ts contains "reading_status" column migration code
    - PRAGMA table_info(books) check pattern present
    - ALTER TABLE statement adds column with DEFAULT 'want_to_read'
    - database.save() called after migration
  </acceptance_criteria>
  <verify>
    <automated>cd backend && npx tsc --noEmit 2>&1 | grep -c "error" | xargs -I {} test {} -eq 0 && echo "TypeScript OK"</automated>
  </verify>
  <done>reading_status column migration added to initDatabase function, compiles without errors</done>
</task>

<task type="auto">
  <name>Task 2: Add reading status validation schemas</name>
  <files>backend/src/validators/schemas.ts</files>
  <read_first>
    - backend/src/validators/schemas.ts (existing schema patterns)
    - .planning/phases/15-reading-status/15-RESEARCH.md (section 2: API Endpoints, validation schema)
  </read_first>
  <action>
    Add two validation schemas following existing patterns in the file:
    
    1. Add readingStatusSchema after collectionSchema (around line 22):
       - z.object with status field
       - status: z.enum(['want_to_read', 'reading', 'read']) with Chinese error message
    
    2. Update bookQuerySchema (around line 50) to add status filter:
       - Add status: z.enum(['want_to_read', 'reading', 'read']).optional()
       - Place after collection_id field
    
    3. Export types if needed (readingStatusSchema type not required, inline is fine).
  </action>
  <acceptance_criteria>
    - readingStatusSchema exported with z.enum containing exactly: 'want_to_read', 'reading', 'read'
    - bookQuerySchema contains status field with same enum values
    - Chinese error message for invalid status (matching collectionSchema pattern)
  </acceptance_criteria>
  <verify>
    <automated>cd backend && npx tsc --noEmit 2>&1 | grep -c "error" | xargs -I {} test {} -eq 0 && echo "TypeScript OK"</automated>
  </verify>
  <done>readingStatusSchema and updated bookQuerySchema with status filter exported</done>
</task>

<task type="auto">
  <name>Task 3: Add PUT /:id/status endpoint and status filter</name>
  <files>backend/src/routes/books.ts</files>
  <read_first>
    - backend/src/routes/books.ts (existing route patterns, especially PATCH /:id)
    - backend/src/validators/schemas.ts (schemas to import)
    - .planning/phases/15-reading-status/15-RESEARCH.md (section 2: API Endpoints)
  </read_first>
  <action>
    Add status update endpoint and filter support:
    
    1. Import readingStatusSchema at top of file (add to existing schema imports)
    
    2. Add PUT /:id/status endpoint (place after PATCH /:id, around line 190):
       - Use validateParams(idParamSchema) and validateBody(readingStatusSchema)
       - No authMiddleware (per RESEARCH.md recommendation)
       - Get book by id, return 404 if not found
       - UPDATE books SET reading_status = ? WHERE id = ?
       - Call database.save()
       - Return updated book
    
    3. Update GET / endpoint (around line 69) to support status filter:
       - Extract status from req.query
       - Add condition to WHERE clause: b.reading_status = ?
       - Add status param to params array when present
       - Update count query similarly
    
    Follow the existing pattern from collection_id filter implementation.
  </action>
  <acceptance_criteria>
    - Import statement includes readingStatusSchema
    - PUT /:id/status endpoint exists with validateParams, validateBody middleware
    - GET / endpoint handles status query parameter
    - Status filter uses b.reading_status = ? in SQL WHERE clause
    - Both main query and count query include status filter
    - Error responses use Chinese messages (matching existing patterns)
  </acceptance_criteria>
  <verify>
    <automated>cd backend && npx tsc --noEmit 2>&1 | grep -c "error" | xargs -I {} test {} -eq 0 && echo "TypeScript OK"</automated>
  </verify>
  <done>PUT /api/books/:id/status and GET /api/books?status=X endpoints working</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client→API | Untrusted user input via query params and request body |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | PUT /:id/status | mitigate | Zod enum validation restricts to valid status values only |
| T-15-02 | Tampering | GET /?status=X | mitigate | Zod enum validation on query param |
| T-15-03 | Information Disclosure | GET /books | accept | No PII in reading status, low-value target |
</threat_model>

<verification>
1. TypeScript compilation succeeds for all modified files
2. Database migration runs without error on fresh start
3. PUT /api/books/:id/status with valid status returns 200
4. PUT /api/books/:id/status with invalid status returns 400
5. GET /api/books?status=reading returns only reading books
</verification>

<success_criteria>
- reading_status column exists in books table with default 'want_to_read'
- PUT /api/books/:id/status endpoint validates and updates status
- GET /api/books?status=X filters by reading status
- All changes follow existing code patterns and Chinese error messages
</success_criteria>

<output>
After completion, create `.planning/phases/15-reading-status/15-01-SUMMARY.md`
</output>
