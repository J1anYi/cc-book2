---
phase: 15-reading-status
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - frontend/src/api/books.ts
  - frontend/src/views/Library.vue
  - frontend/src/views/Reader.vue
autonomous: true
requirements:
  - STAT-02
  - STAT-03
  - STAT-04

must_haves:
  truths:
    - "Frontend Book interface includes reading_status field"
    - "Library view has status filter dropdown showing all status options"
    - "Opening a book with status want_to_read automatically changes it to reading"
    - "Status filter changes the book list to show only matching books"
  artifacts:
    - path: "frontend/src/api/books.ts"
      provides: "updateReadingStatus function and reading_status in Book interface"
      exports: ["updateReadingStatus", "Book"]
    - path: "frontend/src/views/Library.vue"
      provides: "Status filter dropdown in header"
      contains: "selectedStatus"
    - path: "frontend/src/views/Reader.vue"
      provides: "Auto-status update in onMounted"
      contains: "updateReadingStatus"
  key_links:
    - from: "frontend/src/views/Library.vue"
      to: "frontend/src/api/books.ts"
      via: "getBooks with status param"
      pattern: "getBooks.*status"
    - from: "frontend/src/views/Reader.vue"
      to: "frontend/src/api/books.ts"
      via: "updateReadingStatus import and call"
      pattern: "updateReadingStatus"
---

<objective>
Implement frontend reading status support: API client functions, Library status filter, and Reader auto-status update.

Purpose: Enable users to filter books by reading status and have status automatically update when opening books.
Output: Working frontend UI for reading status management.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-reading-status/15-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add reading_status to Book interface and update API client</name>
  <files>frontend/src/api/books.ts</files>
  <read_first>
    - frontend/src/api/books.ts (current Book interface and getBooks function)
    - .planning/phases/15-reading-status/15-RESEARCH.md (section 3.4: books.ts API Client)
  </read_first>
  <action>
    Update the API client for reading status support:
    
    1. Add reading_status field to Book interface (around line 16):
       - reading_status: string (place after tags field)
    
    2. Update getBooks function signature and implementation:
       - Add third parameter: status?: string
       - Add status to params object when provided: if (status) params.status = status
    
    3. Add new updateReadingStatus function after updateBook function:
       - Parameters: id: number, status: 'want_to_read' | 'reading' | 'read'
       - Return type: Promise&lt;Book&gt;
       - Use api.put(`/books/${id}/status`, { status })
       - Return response.data
  </action>
  <acceptance_criteria>
    - Book interface contains reading_status: string field
    - getBooks accepts optional third parameter status
    - getBooks passes status to API params when provided
    - updateReadingStatus function exported with correct signature and return type
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vue-tsc --noEmit 2>&1 | grep -c "error" | xargs -I {} test {} -eq 0 && echo "TypeScript OK"</automated>
  </verify>
  <done>Book interface updated, getBooks supports status filter, updateReadingStatus function exported</done>
</task>

<task type="auto">
  <name>Task 2: Add status filter dropdown to Library view</name>
  <files>frontend/src/views/Library.vue</files>
  <read_first>
    - frontend/src/views/Library.vue (current filter structure and handleCollectionChange pattern)
    - .planning/phases/15-reading-status/15-RESEARCH.md (section 3.1: Library.vue)
  </read_first>
  <action>
    Add reading status filter to Library header:
    
    1. Add selectedStatus ref after selectedCollection (around line 104):
       - const selectedStatus = ref&lt;string | null&gt;(null)
    
    2. Add status filter dropdown in template after collection filter (around line 29):
       - Use same filter-box structure as collection filter
       - v-model="selectedStatus" @change="handleStatusChange"
       - Options: value="" for "全部状态", "want_to_read" for "📖 想读", "reading" for "📚 在读", "read" for "✅ 已读"
    
    3. Add handleStatusChange function after handleCollectionChange (around line 182):
       - Call getBooks(undefined, undefined, selectedStatus.value || undefined)
       - Update books.value with result
       - Handle errors with console.error
    
    4. Update handleCollectionChange to preserve status filter:
       - Pass selectedStatus.value as third param: getBooks(undefined, selectedCollection.value, selectedStatus.value || undefined)
  </action>
  <acceptance_criteria>
    - selectedStatus ref declared with type string | null
    - Status filter dropdown in template with three options plus "全部状态"
    - handleStatusChange function calls getBooks with status param
    - handleCollectionChange preserves status filter when changing collection
    - Options display with emoji icons: 📖, 📚, ✅
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vue-tsc --noEmit 2>&1 | grep -c "error" | xargs -I {} test {} -eq 0 && echo "TypeScript OK"</automated>
  </verify>
  <done>Status filter dropdown functional, filters book list by reading status</done>
</task>

<task type="auto">
  <name>Task 3: Add auto-status update to Reader view</name>
  <files>frontend/src/views/Reader.vue</files>
  <read_first>
    - frontend/src/views/Reader.vue (current onMounted and imports)
    - frontend/src/api/books.ts (updateReadingStatus function to import)
    - .planning/phases/15-reading-status/15-RESEARCH.md (section 3.2: Reader.vue Auto Status Update)
  </read_first>
  <action>
    Add automatic reading status update when opening books:
    
    1. Add updateReadingStatus to imports from api/books (around line 121):
       - Add to existing import: import { getBook, updateReadingStatus, type Book } from '../api/books'
    
    2. Update onMounted function (around line 150):
       - After book.value = await getBook(bookId.value)
       - Add auto-status logic wrapped in try/catch:
         - Check if book.value?.reading_status === 'want_to_read'
         - If true: await updateReadingStatus(bookId.value, 'reading')
         - Then: book.value.reading_status = 'reading'
       - catch block: console.error, silent failure (no user-facing error)
    
    The auto-update should NOT block or interrupt reading if the API call fails.
  </action>
  <acceptance_criteria>
    - updateReadingStatus imported from api/books
    - onMounted checks book.value?.reading_status === 'want_to_read'
    - Calls updateReadingStatus(bookId.value, 'reading') when condition met
    - Updates local book.value.reading_status after successful API call
    - Errors caught and logged, not thrown to user
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vue-tsc --noEmit 2>&1 | grep -c "error" | xargs -I {} test {} -eq 0 && echo "TypeScript OK"</automated>
  </verify>
  <done>Reader automatically updates status to reading when opening a want_to_read book</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend→Backend API | All API calls are untrusted from server perspective |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04 | Tampering | Auto-status update | accept | Non-critical operation, silent failure acceptable |
| T-15-05 | Information Disclosure | Status filter | accept | No sensitive data in reading status |
</threat_model>

<verification>
1. TypeScript compilation succeeds for all modified files
2. Status filter dropdown appears in Library header
3. Selecting status filter updates book list
4. Opening a want_to_read book triggers status update to reading
5. Status filter and collection filter work together
</verification>

<success_criteria>
- Library view shows status filter dropdown with all three options
- Status filter correctly filters book list via API
- Reader view auto-updates status from want_to_read to reading
- Both filters (collection and status) can be combined
- All TypeScript types are correct
</success_criteria>

<output>
After completion, create `.planning/phases/15-reading-status/15-02-SUMMARY.md`
</output>